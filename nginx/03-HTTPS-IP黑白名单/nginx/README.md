# nginx

[nginx.conf](nginx.conf) 使用三个 `server` 演示入口安全：

- 端口 `80` 把 HTTP 永久跳转到 HTTPS。
- 端口 `443` 加载证书、协商 TLS，并把解密后的请求代理给内部服务。
- 端口 `8088` 分别对管理路径使用白名单、对普通路径使用黑名单。

示例前必须替换 `CERT_ROOT`、测试域名和示例网段。HSTS 会被浏览器长期缓存，IP 规则也可能导致管理入口不可达，因此应先在隔离环境测试并保留回滚通道。

